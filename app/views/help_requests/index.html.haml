.base
  %h3 Help request messages
  .row
    - @help_requests.each do |help|
      .large-4.medium-4.columns
        .card
          .newh
            = link_to help.title, new_workorder_path(help.id)
        = help.message
        %br
        = link_to "Delete", "/help_requests/#{help.id})", method: :delete, class: 'button tiny alert'
